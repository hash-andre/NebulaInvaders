{
  "name": "nebula-invaders",
  "version": "1.0.0",
  "private": true,
  "description": "A dependency-free canvas arcade game with a multi-level boss campaign.",
  "scripts": {
    "check": "node --check script.js",
    "test": "node --test"
  }
}
